# Windows music player implementation

Objective: Implement the approved Windows-first local MP3 TUI incrementally, beginning with a small playback compatibility slice rather than claiming a finished v1.0.

## Problem and scope

The approved behavior lives in `desing/requirements.md`; the magenta OpenPencil mockup is visual guidance. Target Windows 10 22H2 on this host first; Windows 11 remains unverified. `D:\Music` is a fixed root; no installer, song deletion, configurable root, or recursive library. Use Go with standard Go tests; Bubble Tea v2 and Oto v3 are candidates to pin at implementation. `go-mp3` is unmaintained upstream, so record its risk and validate decoding on an actual MP3 before claiming compatibility. Keep source, tests, and user-facing instructions in each work unit. Do not push or open a PR without the user's decision.

## Method and delivery

- TDD: strict RED → GREEN → REFACTOR, explicitly selected by the user in this session. Runner: `go test ./...` (Go standard testing, no project runner yet); build: `go build ./...`. Record exact observed output for each task.
- Feature branch: `feat/windows-music-player`, branched from `main` at `f8d0e94`. Close each verified task with a Conventional Commit work unit; record its identity here.
- Delivery strategy: ask-on-risk; user selected stacked/linked PRs (each PR based on the previous one). Forecast: full feature exceeds ~400 authored changed lines across multiple work units. Track authored additions + deletions from commits, excluding generated assets; split coherent units, not tests from behavior, and do not shrink code to fit. No PR created.
- Native RDD is on (global); review candidate is a commit or PR slice, not a task checkbox. Follow native risk assessment and the offered route only.

## Tasks

- [ ] I1: Prove a bounded MP3 playback engine on Windows: open/decode, play/pause/stop-and-close, natural next in active folder, fixed alphabetical ordering; isolate device so tests do not need speakers. Check: observed RED before GREEN; `go test ./...`, `go build ./...`; manual audio check only with an available MP3 and output device. No claim of audible playback until actually heard. Suggested surfaces: `go.mod`, `go.sum`, `internal/playback/`, `cmd/music/`, `README.md`.
- [x] I2: Discover immediate folders and MP3 files under `D:\Music`, sort deterministically, report empty/unreadable cases without false success; keep playback queue independent from browsing. Check: Go tests with temporary folders and `go test ./...`.
- [ ] I3: Implement two-pane terminal UI using approved keyboard map and magenta visual direction; wire browsing and playback status. Check: model/update tests, build, interactive CMD and PowerShell smoke check.
- [ ] I4: Add safe folder create/rename/recycle and MP3 base-name rename with in-use guard; never permanently delete on recycle failure. Check: filesystem tests (mock recycle boundary), Windows integration test with disposable files, build.
- [ ] I5: Verify v1 scenarios, Windows terminal/runtime behavior, and portable `music.exe` usage; update documentation and remaining OD4 evidence. Check: full Go tests/build plus manual playback and CMD/PowerShell acceptance on Windows; record untested platforms honestly.

## Progress

- I1 in progress: worker added pinned Go module, testable playback engine, explicit-path probe, README and tests; observed RED for stale error and asynchronous read failure, then `go test ./...`, `go build ./...`, `git diff --check` green. A user-supplied MP3 now exists directly inside `D:\Music`; a bounded explicit-path probe opened and stopped it with exit code 0, without modifying it. Audible playback is not yet human-confirmed.
- Independent verification: `go test ./...`, `go build ./...`, `go vet ./...` passed. `go test -race ./internal/playback` could not run with CGO disabled. Verifier found a pause/completion interleaving and asynchronous-error visibility defect; fix and reverify before I1 can close. Native read-only assess returned unassessable (empty native output), so independent verification was required.
- Correction: observed new RED before fixes to pause/completion synchronization and asynchronous error notifications; worker reports `go test ./...`, `go build ./...`, `go vet ./...` green, and a subsequent formatting-only pass reported `gofmt -l` empty. Parent independently reran `go test ./internal/playback -count=1` and `go build ./...` successfully. Human audible check remains pending.
- Work-unit commit `0d6dc7a` (`feat(playback): define ordered queue and lifecycle`) contains the 387-line pure playback state machine, its tests, and pinned module; rollback boundary is those four files. Native committed-range assess returned unassessable (empty output); inspect/start for that committed range was blocked at an empty workspace/base-ref selection before lineage creation, so no native review receipt is claimed. Independent verification and Go tests remain the fallback evidence; do not retry START blindly.
- User chose stacked PRs; the first coherent core unit has 387 authored lines. Next coherent slice is Oto adapter + runnable probe + documentation, expected <400 authored lines; no PR created or remote push.
- Work-unit commit `d6f0823` adds the Oto MP3 adapter, explicit-path CLI probe, tests, README, and this task file (365 authored lines). Independent verifier found no severe regression in pause/completion or asynchronous error reporting; `go test ./... -count=1`, `go build ./...`, `go vet ./...` passed. Its one remaining synchronous Stop error gap was fixed test-first in commit `0c86b64` (30 authored diff lines), with fresh independent verification and the same focused checks passing. Rollback boundaries are the named files per commit; second stacked review slice is `d6f0823` + `0c86b64` (~395 authored lines) based on `0d6dc7a`. No native receipt: assess for both later commits returned schema-incompatible/unassessable and used independent verification instead. No push/PR.
- I1 implementation complete but acceptance remains open until a human listens to the user-provided MP3 and verifies pause/stop; no false claim of audible output. I2 can proceed independently on the same feature branch while that manual check waits.
- I2 implemented test-first in `internal/library/`: only immediate directories and directly contained MP3s, deterministic playback-compatible order, empty states, actionable root errors, partial playlist warnings retaining accessible entries, and symlink exclusion. Worker observed RED for scanner API and partial-return regression, then `go test ./...`, `go build ./...`, `go vet ./...`, and gofmt check passed; parent reran focused tests, independent verifier reran all commands with no severe finding. No real-library scan or manual Windows symlink test claimed. Rollback boundary: `internal/library/library.go`, `internal/library/library_test.go`, and the I2 checkbox/progress update in this task file.
- I2 work-unit commit `df8bd4c` (`feat(library): discover immediate MP3 playlists`) has 253 authored diff lines including task progress; rollback boundary is the two scanner files plus this progress update. `go test ./... -count=1` passed before commit; committed-range native assess returned unassessable/schema-incompatible, independent verifier already checked the scanner candidate. No receipt or remote push.
- I3 model slice in progress: Bubble Tea v2 pinned; testable `internal/ui/` handles two-pane responsive navigation, folder selection, playback keys, root/empty/warning states, and sanitizes untrusted display text. Strict TDD observed RED for API, keyboard, pane layout and terminal-cell/sanitization cases before GREEN; `go test ./... -count=1`, `go build ./...`, `go vet ./...` passed. Independent verifier found unsafe root-error rendering, narrow/Unicode layout, and weak queue test; worker corrected all three and parent reran tests. No CLI wiring or interactive CMD/PowerShell smoke check yet.
- One UI writer call timed out after a bash operation and produced an out-of-scope `.codegraph/` index; parent diagnosed a failed RED test and no live worker, removed only that generated index, then the resumed writer finished the layout. Cause of the stall is unconfirmed; no repository source was lost.
- The cohesive UI model, its tests and pinned dependencies forecast about 490 authored lines, above the ~400 review heuristic. Do not omit tests or compress code; report an honest size exception if no clean behavior/test split fits one slice. No PR or remote push.
- Independent verifier reran Go tests/build/vet and found the corrected UI model free of the earlier deterministic findings. Work-unit commit `5f6ee78` (`feat(ui): add testable two-pane library view`) has 492 authored diff lines, the smallest coherent model+tests+dependency unit after one honest slicing pass; it needs a `size:exception` if proposed as a standalone PR slice. Native committed-range assess returned unassessable/schema-incompatible, so no native receipt; independent verification is fallback evidence. No push or PR.
- I3 executable wiring implemented test-first: default `music` launches the fixed-root TUI; `music --probe <mp3 path>` retains the I1 direct-file check; idle playback notices refresh status, `q`/Ctrl+C stop playback and surface release errors. Independent verifier identified missing Ctrl+C shutdown; writer added observed RED test and corrected it. `go test ./... -count=1`, `go build ./...`, `go vet ./...`, gofmt and diff whitespace checks passed. Interactive CMD/PowerShell smoke and audible playback are still unverified, so I3 acceptance stays open.
- I3 wiring work-unit commit `418ed74` (`feat(ui): launch fixed-root music TUI`) contains 189 authored diff lines including task progress; rollback boundary is the CLI, UI, README and progress edits. Committed-range assess was unavailable/schema-incompatible; fresh independent verifier reran Go tests/build/vet and found no severe deterministic defect. No native receipt, interactive terminal proof, push or PR.
- I4a test-first service added under `internal/manage/`: create/rename immediate playlist and rename direct-child MP3 base (preserving `.mp3`), reject Windows-invalid/reserved/colliding names, symlinks and traversal; block active/paused track and its containing playlist until Stop. New `playback.Engine.ActivePath()` was committed separately as `6f5c17f` (49 authored lines), with focused tests, committed-range assess unavailable and independent verifier passing. Manager tests use temporary folders and a fake audio backend; independent verifier found an initial active-folder rename bypass, writer corrected it, and fresh verifier found no severe deterministic residual. `go test ./... -count=1`, `go build ./...`, `go vet ./...` passed. Overwrite safety under external concurrent filesystem mutation is not proven; no real `D:\Music` changes.
- I4a service committed as `05a6ed0` (`feat(library): safely rename playlists and tracks`, 392 authored diff lines) after green Go tests; committed-range native assess was unavailable/schema-incompatible, and the independent verifier found no deterministic severe residual. Rollback boundary is `internal/manage/` and the I4a progress edit; no push or PR.
- I4 remains open: UI forms and Windows Recycle Bin backend with explicit confirmation are not wired. Avoid any permanent delete fallback.
- Session handoff: user confirmed `go run ./cmd/music` launches from `D:\work\music-cli` in CMD; PowerShell and interactive key/exit behavior were not confirmed. Their `--probe` command omitted quotes around an MP3 path with spaces, so the CLI split it into multiple paths and returned `all paths must be in one folder` BEFORE opening audio. This is a command-line quoting error, not evidence of silence or an audio defect. Next manual check: from the repository in CMD run `go run ./cmd/music --probe "D:\Music\<exact filename>.mp3"`, listen, pause/resume with Enter, stop with `s`, exit with `q`; never claim audio until the user confirms it.
- Stop requested by user due to context/token budget. Next implementation: I4 safe UI forms; research and validate Recycle Bin behavior using disposable fixtures only; then I5 Windows acceptance and packaging. Native RDD assessments have been unavailable and no review receipt exists; run required independent verification on new candidates. Current branch `feat/windows-music-player` has no push/PR. An unexplained zero-byte untracked file `go` appeared at repo root at handoff; do not delete or commit it without establishing ownership.
