# Magenta terminal layout

Objective: Bring the live Windows terminal UI visually close to `desing/magenta-preview/01-Music — Library view.png` after the user confirmed the current plain text layout is not acceptable.

## Scope and constraints

- Retain the approved keyboard map, fixed `D:\Music` library behavior, safe dialogs, and playback semantics. Behavior in `desing/requirements.md` takes precedence over illustrative sample content and progress.
- Recompose the header, boxed two-pane library, now-playing panel, and control footer with magenta emphasis and dark background. Reflect actual playlists and tracks, not sample data.
- Never show fabricated elapsed time, duration, or playback progress: the current Player interface does not expose them. Show truthful stopped/active status instead.
- Handle narrow/short terminals without ANSI injection, broken cell widths, or hiding actionable dialogs and errors. Preserve Unicode width clipping and sanitized untrusted text.
- TDD mode: strict RED → GREEN → REFACTOR, inherited from the existing feature session. Runner: `go test ./internal/ui -count=1`, then `go test ./... -count=1`, `go build ./...`, `go vet ./...`.
- Delivery: user authorized one cohesive work-unit commit despite ~715 authored lines (accepted size exception); no push or PR. `music.exe` is untracked; preserve untracked binaries, `go`, and `.codegraph/`.

## Tasks

- [x] V1: Render a truthful boxed header, folder/track library, playback status, and footer using existing model state. Test focus, selected folder, actual names/counts, stopped and active playback, dialogs/errors, sanitization and cell widths. Expected surfaces: `internal/ui/model.go`, `internal/ui/model_test.go`.
- [x] V2: Adapt panel layout to terminal height and width (short/narrow windows), keeping dialogs, errors, and key help reachable; exercise resize and boundary tests. Expected surfaces: `internal/ui/model.go`, `internal/ui/model_test.go`.
- [x] V4: Add reference-like vertical spacing and visual hierarchy at roomy terminal sizes: flexible library pane height, pane-local action hints, restrained magenta accent versus neutral content, while preserving compact rendering, truthful playback and recycle safety. Test roomy/compact/resize views and unchanged behavior; expected surfaces: `internal/ui/model.go`, `internal/ui/model_test.go`.
- [x] V3: Verify focused/full Go tests and build/vet, inspect live visual output against the reference without claiming exact pixel identity; document skipped manual terminal checks and decide whether to replace the untracked `music.exe` with the user.

## Progress

- User approved visual redesign after comparing `C:/Users/User/Downloads/grbeie.JPG` with the magenta concept. Read-only mapping found `internal/ui/model.go` currently renders unboxed text, stores width only, and has no timing/progress data; CLI startup does not need to change.
- V1 writer added bordered magenta header, panes, playback status, and footer in `internal/ui/model.go` plus structural tests in `internal/ui/model_test.go`. RED `go test ./internal/ui -run TestMagentaLayoutStructure -count=1` failed before implementation; focused/full tests, build, and vet passed after implementation. Independent verifier reran all four commands successfully but found severe one-cell overflow, confirmation/error clipping, and dialogs hidden below short viewports. V1 remains open pending V2 corrections; no commit or executable rebuild.
- V1/V2 correction sequence resolved compact clipping and selection visibility, short error/root guidance, and fail-closed recycle confirmation when the complete name and destination cannot be displayed. Resize clears a prior Move selection. Tests were added RED before GREEN for the original layout and each correction. Final independent verifier found no concrete severe residual in model/test logic; `go test ./internal/ui -count=1`, `go test ./... -count=1`, `go build ./...`, and `go vet ./...` passed. Parent spot-reran focused UI tests and `git diff --check` (only CRLF conversion warnings). The two UI files total 544 authored diff lines, above the ~400 review heuristic; tests remain with behavior. No commit or binary rebuild. Live terminal visual comparison remains pending.
- US4 follow-up: user flagged missing `contents` wording and a coverage caveat. Added test-first explicit named-folder-and-contents Windows Recycle Bin text and actual key-selection then `WindowSizeMsg` reset test. Independent verification exposed an 80×2 feedback retry where a hidden Move could still execute; added failing regression and blocked retries whenever the compact four-line prompt cannot fit. Final independent read-only check found no concrete severe residual; focused/full Go tests, build, and vet passed. No executable rebuild or live terminal inspection.
- V3: after user asked what to do, built a separate `music-preview.exe` with `go build -o music-preview.exe ./cmd/music` (exit 0; file exists) so the old untracked `music.exe` remains untouched. Awaiting user's live PowerShell screenshot/visual acceptance. No commit, push, or PR.
- User's `fffeewf.JPG` preview shows the new borders, but library panes are shallow and action hints/contrast differ from the reference. User explicitly approved a second pass for proportions and hierarchy. Screenshot terminal is about 10–14 rows high versus the reference's roughly 28–30; tall layout needs a taller window and compact layout must remain safe.
- V4 added height-filled roomy panes, pane-local hints and restrained magenta accents with test-first RED (`TestRoomyLibraryHeightAndHints` at 100×30 showed only 15 rows) then GREEN. Independent verification found compact 100×12 footer/status and 24×5 retry feedback defects; corrected test-first, then full/focused tests, build and vet passed. Final verifier reported a 24×5 prompt cannot fully fit and is replaced by `resize to confirm` and Cancel with Move blocked; this is intentional fail-closed behavior, not approval to recycle. Parent reran `go test ./internal/ui -count=1` and `git diff --check` (CRLF warnings only). Aggregate UI diff is 717 authored lines; keep behavior and tests together, no commit/PR. Built separate `music-preview-v2.exe` (exit 0), preserving both previous preview and `music.exe`. Live V4 visual acceptance pending in a taller PowerShell window.
- User ran `music-preview-v2.exe` in a tall PowerShell window and supplied `C:/Users/User/Downloads/gfrgdsf.JPG`. It visibly shows tall two-pane library, pane-local controls, separate stopped playback panel and footer; user said “me gusta”. Visual acceptance of V4 is user-reported. No playing-state, long-list interactive, recycle integration, or CMD check inferred from screenshot.
- User explicitly approved replacing the executable. Copied the old untracked `music.exe` to `music-previous.exe`, then replaced `music.exe` from `music-preview-v2.exe`; SHA-256 of new binary and preview both `93230db2f92086909f160c6fa169aa90b37f2981d7ff63dffd8bda026a27d486`, backup differs. User subsequently ran `music` in a fresh PowerShell and confirmed “quedó”: PATH command launches the approved new layout. V3 closed by that manual acceptance; interactive playback/recycle/CMD/Windows 11 not reverified by this visual check.
- Delivery verification before commit: `go test ./internal/ui -count=1`, `go test ./... -count=1`, `go build ./...`, `go vet ./...`, and `git diff --check` passed (LF→CRLF warnings only). Independent read-only inspection found no natural functional split with tests alongside behavior. User explicitly accepted one cohesive oversized commit; no push or PR authorized. Commit identity is recorded in the Engram feature mirror and delivery report after creation to avoid a self-referential commit hash.
